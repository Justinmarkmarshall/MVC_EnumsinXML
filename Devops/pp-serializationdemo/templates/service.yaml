apiVersion: v1
kind: Service
metadata:
  name: {{ include "pp-serializationdemo.fullname" . }}
  labels:
    {{- include "pp-serializationdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pp-serializationdemo.selectorLabels" . | nindent 4 }}
