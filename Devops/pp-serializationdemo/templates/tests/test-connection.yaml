apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pp-serializationdemo.fullname" . }}-test-connection"
  labels:
    {{- include "pp-serializationdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pp-serializationdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
